name: orchestrator_riverpod
description: Creates an OrchestratorNotifier with State (Riverpod integration)
version: 0.1.0
vars:
  name:
    type: string
    description: The name of the notifier (e.g., User)
    prompt: What is the notifier name?
