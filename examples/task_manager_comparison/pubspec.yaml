name: task_manager_comparison
description: Side-by-side comparison of Traditional vs Orchestrator patterns

publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.7.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0
  orchestrator_bloc:
    path: ../../packages/orchestrator_bloc
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
